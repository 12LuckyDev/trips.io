import React from "react";
import { CustomFileInput } from "@custom-components";

const FancyFileInput = ({ ...props }) => {
	return <CustomFileInput {...props} />;
};

export default FancyFileInput;
