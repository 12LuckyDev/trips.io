const DEFAULT = {
  colors: {
    backgroud: "#ffffff",
    primary: "#515752",
    secondary: "#ffffff",
  },
};

const DARK = {
  colors: {
    backgroud: "#515752",
    primary: "#ffffff",
    secondary: "#515752",
  },
};

const THEMES = {
  DEFAULT,
  DARK,
};

const THEME_NAMES = {
  DARK: "DARK",
  DEFAULT: "DEFAULT",
};

export { THEMES, THEME_NAMES };
