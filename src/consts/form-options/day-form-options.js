import * as DAY_FORM_TYPES from "./day-form-types";

const DAY_FORM_OPTIONS = [
	{ id: DAY_FORM_TYPES.SINGLE, name: "Single day" },
	{ id: DAY_FORM_TYPES.RANGE, name: "Range of days" },
];

export { DAY_FORM_OPTIONS, DAY_FORM_TYPES };
