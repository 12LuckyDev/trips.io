import * as DIRECTION_TYPES from "./direction-types";

const DIRECTION_OPTIONS = [
	{ id: DIRECTION_TYPES.LEFT, name: "Left" },
	{ id: DIRECTION_TYPES.RIGHT, name: "Right" },
];

export { DIRECTION_OPTIONS, DIRECTION_TYPES };
